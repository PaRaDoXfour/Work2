import math
from mod import sum_of_digits
from mod import input_number

def main():
    # Вводимо число n
    n = input_number("Введіть число n: ")
    # Знаходимо суму його цифр
    digit_sum = sum_of_digits(n)
    # Виводимо результат
    print(f"Сума цифр числа {n}: {digit_sum}")

if __name__ == "__main__":
    main()
import math
from mod import sum_of_digits
from mod import input_number

def main():
    # Вводимо число n
    n = input_number("Введіть число n: ")
    # Знаходимо суму його цифр
    digit_sum = sum_of_digits(n)
    # Виводимо результат
    print(f"Сума цифр числа {n}: {digit_sum}")

if __name__ == "__main__":
    main()